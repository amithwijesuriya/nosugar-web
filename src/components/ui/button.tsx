export function Button({variant="default",className="",onClick,children}:any){const base="px-3 py-2 rounded-xl text-sm border";const styles=variant==="outline"?"bg-white hover:bg-gray-50":variant==="ghost"?"bg-transparent border-transparent hover:bg-gray-100":"bg-black text-white hover:opacity-90";return <button className={`${base} ${styles} ${className}`} onClick={onClick}>{children}</button>}